version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/dotoread-server
permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu
hooks:
  ApplicationStart:
    - location: scripts/start-server.sh
      timeout: 300
      runas: ubuntu